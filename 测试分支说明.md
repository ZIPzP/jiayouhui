# 🧪 测试分支使用说明（dev-test）

> 用途：在不影响线上（main 分支 / 服务器）的情况下，自由改设计、做页面。
> main = 线上正式版；dev-test = 本地测试版。

## 1️⃣ 开始设计（切到测试分支）
```bash
git checkout dev-test
```

## 2️⃣ 本地预览（不用重启，改完刷新即可）
```bash
node server.js
```
浏览器打开 http://localhost:3000

## 3️⃣ 设计完成 → 只把改动的文件合回 main 再上传
```bash
git checkout main
git checkout dev-test -- 改动的文件1 改动的文件2 ...
git add 改动的文件1 改动的文件2 ...
git commit -m "本次改动说明"
git push origin main
```
> 服务器执行：cd /opt/jiayouhui && sudo git pull && sudo pm2 restart jiayouhui

## 4️⃣ 回到测试分支继续改
```bash
git checkout dev-test
```

## 📋 常用查看
- 当前分支：git branch
- 测试分支比 main 多了哪些改动：git diff main --name-only
- 只取单个文件：git checkout dev-test -- public/index.html

## ⚠️ 注意
- 不要用 git add -A 全加（会把 .codewhale / .dsh-vision-router / src 等未跟踪目录也加进去）
- 测试分支的提交不 push 就不影响线上
