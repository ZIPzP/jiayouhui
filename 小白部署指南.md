# 🧭 小白版上线指南（一步一步照着点）

先把三样东西分清楚：
| 东西 | 是什么 | 用途 |
|---|---|---|
| 代码 | 当前这个文件夹 | 你的网站程序 |
| GitHub | 免费"网盘" | 备份代码、方便以后更新（不是必须，但推荐） |
| 阿里云域名 | 你的网址（如 www.xxx.cn） | 只是一个"门牌号"；真正放网页需要一台服务器 |

> 关键：**域名≠服务器**。要让别人能访问，除了域名还需要一台"阿里云轻量应用服务器"（约 24~100 元/月）。

---

## ✅ 第一步：先把代码备份到 GitHub（5 分钟，用图形界面，不用记命令）

1. 注册 GitHub 账号：打开 https://github.com ，点 Sign up，免费注册
2. 下载 **GitHub Desktop**（图形界面版）：打开 https://desktop.github.com → Download → 安装 → 登录你的 GitHub 账号
3. 打开 GitHub Desktop：
   - 菜单 **File → Add local repository**
   - 选择文件夹：`C:\Users\34968\Documents\家庭旅游篇`
   - 点 **Publish repository**（首次发布）→ 仓库名填 `jiayouhui` → 点 **Publish repository**
4. 完成 ✅ 你的代码就备份到 GitHub 了（以后更新：改完点右上角 **Commit** 和 **Push origin**）

> 不会命令没关系，GitHub Desktop 全是按钮。这一步做完，代码就有了"保险"。

## ✅ 第二步：准备一台服务器

- 到阿里云买 **轻量应用服务器**（2核2G 即可），系统选 **Ubuntu 22.04**，地区选离你近的（如华东/华南）
- 买好后在阿里云控制台找到服务器的 **公网 IP** 和 **root 密码**（记下来）
- 同时到 **阿里云域名控制台 → 云解析** 添加两条 A 记录（先加，生效要几分钟）：
  - 主机记录 `@` → 填服务器公网 IP
  - 主机记录 `www` → 填服务器公网 IP

## ✅ 第三步：装"宝塔面板"（图形化，比命令行简单 100 倍）

1. 在阿里云控制台点服务器的 **远程连接**（或电脑上装 Xshell 连 SSH）
2. 登录后粘贴这一行，回车（等 1-3 分钟自动装完）：
```
wget -O install.sh https://download.bt.cn/install/install-ubuntu_6.0.sh && bash install.sh
```
3. 装完屏幕会显示一个 **网址 + 用户名 + 密码**（截图保存）
4. 用浏览器打开那个网址 → 登录宝塔面板

## ✅ 第四步：在宝塔里装软件 + 传代码

1. 宝塔左侧 **软件商店** → 安装 **Node.js（20.x）** 和 **Nginx**
2. 左侧 **文件** → 进入 `/www/wwwroot` → 把 `家游汇-发布版.zip`（我打包好的）上传 → 右键解压 → 得到一个文件夹（如 `jiayouhui`）
3. 左侧 **网站** → 添加站点 → 域名填你的域名（如 `www.xxx.cn`）→ 根目录选刚才解压的 `jiayouhui` 文件夹 → 提交

## ✅ 第五步：启动程序 + 反向代理 + HTTPS

1. 宝塔左侧 **网站** → 你的站点 → **设置 → 反向代理** → 添加：
   - 名称：`jiayouhui`，目标 URL：`http://127.0.0.1:3000` → 保存
2. 宝塔左侧 **文件** → 进入项目文件夹 → 右上角 **终端**，逐条粘贴运行：
```
npm i -g pm2
node tools/set-key.js sk-你的DeepSeekKey
node tools/set-passcode.js 你的口令
pm2 start ecosystem.config.js
pm2 save
```
3. 宝塔左侧 **网站** → 你的站点 → **SSL** → 申请 Let's Encrypt 免费证书 → 开启强制 HTTPS
4. 浏览器打开 `https://你的域名` → 输入访问口令 → 🎉 上线成功！

## 🆘 常见问题
- **打不开 / 502**：多半是 pm2 没启动，回终端运行 `pm2 restart jiayouhui`；或反向代理没配对
- **一直转圈**：先确认 `node tools/set-key.js` 填的是真 Key；再看宝塔终端 `pm2 logs jiayouhui` 的报错
- **不需要口令**：跳过 `set-passcode` 那行即可（但不推荐公网裸奔）
- **忘了口令**：服务器终端运行 `node tools/set-passcode.js 新口令`

> 需要更细的图文/远程协助？把你的情况告诉我（有没有买服务器、卡在哪一步），我继续帮你。