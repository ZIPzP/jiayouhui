# 🛠️ 家游汇 · 运维手册（服务器版）

> 服务器：阿里云 华东1(杭州) · 2核2G · Ubuntu 22.04
> 公网 IP：120.55.44.25
> 访问地址：http://120.55.44.25（nginx 80 端口反代到 127.0.0.1:3000，直连服务器；域名备案通过后切换）
> 程序目录：/opt/jiayouhui

---

## 1️⃣ 怎么看网站是否正常

在**服务器终端**执行：

```bash
sudo pm2 status                    # 看网站服务（jiayouhui 应为 online）
sudo systemctl status nginx --no-pager -l   # 看 nginx 反代（应为 active running）
```

手机/电脑打开 **http://120.55.44.25** 输入口令能进 = 一切正常。
（nginx 反代配置：/etc/nginx/sites-available/jiayouhui，监听 80 → 127.0.0.1:3000）

---

## 2️⃣ 更新网站（改完代码后）

家里电脑改好 → 推到 GitHub → 在服务器执行：

```bash
cd /opt/jiayouhui
sudo git pull
sudo pm2 restart jiayouhui
```

> ⚠️ 服务器直连 GitHub 可能被墙，仓库用的是国内镜像（ghfast.top），一般能拉下来。
> 如果 `git pull` 失败，改用：`sudo git pull https://ghfast.top/https://github.com/ZIPzP/jiayouhui.git`

---

## 3️⃣ 改访问口令（推荐：直接改配置文件，立即生效）

邀请码存在服务器上的 **`/opt/jiayouhui/config.local.json`** 文件里。改这个文件**不需要重启服务器**，保存后立刻生效。

**方法一：粘贴一行命令（最不容易错）**
```bash
cd /opt/jiayouhui && sudo node tools/set-passcode.js 你的新口令
```

**方法二：手动改配置文件**
```bash
sudo nano /opt/jiayouhui/config.local.json
```
把 `"passcode": "007026"` 这一行改成你的新口令（例如 `"passcode": "123456"`），然后：
- 按 `Ctrl + X` → 按 `Y` → 按 `回车` 保存退出

> 文件格式参考：项目里已放了一份 `config.local.example.json` 模板。
> ⚠️ 注意：`config.local.json` 里还有 DeepSeek Key，**不要把整个文件发给别人**；`enabled` 保持 `true`，删掉 `passcode` 就等于关闭口令（不推荐公网裸奔）。

---

## 4️⃣ 改 DeepSeek Key

```bash
cd /opt/jiayouhui
sudo node tools/set-key.js sk-你的新key
sudo pm2 restart jiayouhui
```

---

## 5️⃣ 重启 / 开机自启

```bash
sudo pm2 restart jiayouhui     # 重启网站
sudo reboot                    # 重启服务器（重启后 pm2 和 nginx 都会自动恢复）
```

---

## 6️⃣ 常见问题

- **网站打不开**：先 `sudo pm2 status` 看 online 没；再看 `sudo nginx -t` 和 `sudo systemctl status nginx`；最后看 `sudo pm2 logs jiayouhui --lines 30` 有没有报错
- **直接用 IP 也打不开**：检查阿里云安全组是否放行 80 端口；重启 nginx `sudo systemctl restart nginx`
- **Cloudflare 隧道（域名）已弃用**：国内服务器到 Cloudflare 的 UDP 常被网络屏蔽、隧道不稳定，别再依赖它，用 http://120.55.44.25
- **忘记口令**：按上面第 3 条重新设置
- **AI 一直转圈**：`sudo pm2 logs jiayouhui --lines 30`，多半是 DeepSeek Key 问题，按第 4 条换

---

## 7️⃣ 待办事项

- [ ] **ICP 备案**：大陆服务器按规定需备案（阿里云控制台搜「ICP备案」提交）。备案通过后：把域名 familytravelhublz.top 的解析改到 120.55.44.25（A 记录），再申请 Let's Encrypt 免费 HTTPS，即可用域名直连（更快更稳，彻底绕开 Cloudflare 隧道）
- [ ] **（可选）OSS 存图片**：代码已支持，把 public/images 传到 OSS，改 config.json 的 imageBase 为 OSS 域名即可

---

## 📌 信息备忘

- 邀请码/访问口令：`007026`
- Cloudflare 隧道已弃用（不稳定）；当前入口：http://120.55.44.25
- 本地备份：家里电脑 D:\家庭旅游篇（GitHub 也有完整备份）
