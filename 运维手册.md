# 🛠️ 家游汇 · 运维手册（服务器版）

> 服务器：阿里云 华东1(杭州) · 2核2G · Ubuntu 22.04
> 公网 IP：120.55.44.25
> 域名：https://familytravelhublz.top（走 Cloudflare 隧道）
> 程序目录：/opt/jiayouhui

---

## 1️⃣ 怎么看网站是否正常

在**服务器终端**执行：

```bash
sudo pm2 status                    # 看网站服务（jiayouhui 应为 online）
sudo systemctl status cloudflared --no-pager -l   # 看隧道（应为 active running）
```

手机打开 https://familytravelhublz.top 输入口令 `007026` 能进 = 一切正常。

---

## 2️⃣ 更新网站（改完代码后）

家里电脑改好 → 推到 GitHub → 在服务器执行：

```bash
cd /opt/jiayouhui
sudo git pull
sudo pm2 restart jiayouhui
```

> ⚠️ 服务器直连 GitHub 可能被墙，仓库用的是国内镜像（ghfast.top），一般能拉下来。
> 如果 `git pull` 失败，改用：`sudo git pull https://ghfast.top/https://github.com/ZIPzP/jiayouhui.git`

---

## 3️⃣ 改访问口令

```bash
cd /opt/jiayouhui
sudo node tools/set-passcode.js 新口令
sudo pm2 restart jiayouhui
```

---

## 4️⃣ 改 DeepSeek Key

```bash
cd /opt/jiayouhui
sudo node tools/set-key.js sk-你的新key
sudo pm2 restart jiayouhui
```

---

## 5️⃣ 重启 / 开机自启

```bash
sudo pm2 restart jiayouhui     # 重启网站
sudo reboot                    # 重启服务器（重启后网站、隧道、nginx 都会自动恢复）
```

---

## 6️⃣ 常见问题

- **网站打不开**：先 `sudo pm2 status` 看 online 没；再看 `sudo pm2 logs jiayouhui --lines 30` 有没有报错
- **域名打不开但 pm2 正常**：`sudo systemctl status cloudflared` 看隧道；重启隧道 `sudo systemctl restart cloudflared`
- **忘记口令**：按上面第 3 条重新设置
- **AI 一直转圈**：`sudo pm2 logs jiayouhui --lines 30`，多半是 DeepSeek Key 问题，按第 4 条换

---

## 7️⃣ 待办事项

- [ ] **ICP 备案**：大陆服务器按规定需备案（阿里云控制台搜「ICP备案」提交），备案通过后可把域名改为直连（更快），或继续用隧道都行
- [ ] **（可选）OSS 存图片**：代码已支持，把 public/images 传到 OSS，改 config.json 的 imageBase 为 OSS 域名即可

---

## 📌 信息备忘

- 邀请码/访问口令：`007026`
- 隧道名：jiayouhui（家里电脑的隧道已停用并移除自启）
- 本地备份：家里电脑 D:\家庭旅游篇（GitHub 也有完整备份）