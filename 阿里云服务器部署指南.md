# 🚀 阿里云服务器部署指南（2核2G · 新手一步步照着点）

> 目标：把「家游汇」从你家电脑搬到阿里云服务器，关电脑也能 24 小时在线。
> 当前状态：代码已备份到 GitHub（https://github.com/ZIPzP/jiayouhui），本地领先的提交已全部推送。

---

## 第 1 步：买服务器（约 10 分钟）

1. 打开阿里云 → 搜索「**轻量应用服务器**」→ 立即购买
2. 按下面选：
   - **地域**：二选一
     - 🇨🇳 大陆（如 华北2/华南1）：访问快，但**必须备案**（域名要绑到这台服务器，1~2 周，免费，阿里云控制台可办）
     - 🇭🇰 香港：**不用备案**，当天就能上线；国内访问略慢一点点，给朋友亲戚用完全够
   - **套餐**：2核 2G（2核2G 足够本项目，程序零依赖、内存上限 300M）
   - **系统镜像**：选 **Ubuntu 22.04**
   - **带宽**：默认 3~5M 即可
   - 时长：先买 1 个月试用，稳定了再续年付
3. 买完记下两样东西：
   - **公网 IP**（例如 47.xx.xx.xx）
   - **root 密码**（在控制台 → 服务器详情 → 重置实例密码，可设成你记得住的）

---

## 第 2 步：放行端口（1 分钟）

阿里云控制台 → 轻量服务器 → 你的服务器 → **防火墙**（或安全组）→ 添加规则：
| 端口 | 协议 | 用途 |
|---|---|---|
| 22 | TCP | SSH 远程连接 |
| 80 | TCP | 网页 HTTP |
| 443 | TCP | 网页 HTTPS |

> 不放开这 3 个口，网站和远程连接都会失败。

---

## 第 3 步：登录服务器（远程连接）

- 方式一（最省事）：阿里云控制台 → 服务器详情 → 点「**远程连接**」→ 输 root 密码 → 进入黑色终端
- 方式二（推荐，以后方便）：电脑装 **Termius** 或 **Xshell**，新建连接填公网 IP + root + 密码

---

## 第 4 步：一键部署（约 5~8 分钟）

在服务器终端里粘贴这一行，回车（整个过程只需回答 4~5 个问题）：

```bash
wget -O setup.sh https://raw.githubusercontent.com/ZIPzP/jiayouhui/main/deploy/server-setup.sh && bash setup.sh
```

脚本会自动：装 Node → 装 pm2 → 下载你的代码 → 然后依次问你：

1. **GitHub 仓库地址**：直接**回车**（已默认填好你的仓库）
2. **DeepSeek API Key**：粘贴你的 key（不显示，粘完回车）——和家里电脑用的是同一个
3. **访问口令**：输入你想让朋友用的口令（建议还是 007026，或你自己定）
4. **你的域名**：输入 `familytravelhublz.top`
5. **是否申请 HTTPS 证书**：输 `y`，再填一个邮箱

看到「🎉 全部完成」就成功了。

---

## 第 5 步：把域名指到新服务器（关键！）

现在你的域名 NS（DNS）托管在 **Cloudflare**，有两种做法，**推荐做法 A**：

### 做法 A：继续用 Cloudflare 解析（推荐，不折腾）
1. 打开 https://dash.cloudflare.com → 你的域名 → **DNS → Records**
2. 找到那两条 **A 记录**（`@` 和 `www`）：
   - 把「内容/IPv4 地址」改成 **你的阿里云服务器公网 IP**
   - **代理状态保持「已代理（橙云）」**——HTTPS 继续由 Cloudflare 免费提供，服务器上不用再装证书
3. 等 1~5 分钟生效

### 做法 B：不用 Cloudflare，直接阿里云解析
1. 阿里云控制台 → 域名 → 云解析 DNS → 添加两条 A 记录：`@` 和 `www` 都指向公网 IP
2. 在 Cloudflare 把域名删除/暂停代理（把橙云点成灰云 DNS only），否则会冲突
3. HTTPS 用脚本里的 Let's Encrypt 证书（第 4 步已选 y）

> ⚠️ 如果你选了**大陆地域**，必须先在阿里云完成**备案**，否则 80/443 会被拦截、域名打不开（备案在阿里云控制台搜「ICP 备案」，按提示拍照传资料，1~2 周）。香港地域没这个问题。

---

## 第 6 步：收尾（家里电脑可以休息了）

1. 用手机访问 `https://familytravelhublz.top`，输入口令 → ✅ 能用
2. 确认没问题后，家里的电脑：
   - 关掉网站服务 + Cloudflare 隧道（任务栏右下角找隧道图标退出，或运行 `停止.bat`）
   - 电脑从此关机也不影响网站
3. 以后想改网站：告诉我 → 我改完推到 GitHub → 服务器上执行一行更新即可：

```bash
cd /opt/jiayouhui && git pull && pm2 restart jiayouhui
```

---

## ❓ 常见问题

- **打不开 / 502**：多半是 pm2 没起来，终端执行 `pm2 restart jiayouhui`；再不行 `pm2 logs jiayouhui` 把日志发我
- **一直转圈**：确认第 4 步填的 DeepSeek Key 是真 key；`pm2 logs jiayouhui` 看报错
- **忘了口令**：终端执行 `node /opt/jiayouhui/tools/set-passcode.js 新口令`
- **想备份**：GitHub 仓库就是备份，`git pull` 永远能拉回最新版
- **费用**：2核2G 轻量约 60~100 元/月（新用户有优惠，年付更便宜）